<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:rf="clr-namespace:RedCorners.Forms;assembly=RedCorners.Forms"
    xmlns:vm="clr-namespace:CookEasy.ViewModels"
    xmlns:views="clr-namespace:CookEasy.Views"
    x:Class="CookEasy.Views.HomePage"
    x:DataType="vm:HomePageViewModel">

    <StackLayout Padding="20,20,20,20" Spacing="15">
        <Label Text="This is a recipe!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand"
                   FontSize="Large"
                   TextColor="Black"
                   x:Name="Title"/>
        <Button Text="{Binding RecipeText}" CornerRadius="20" Command="{Binding GoToRecipe}"/>
    </StackLayout>
</ContentPage>